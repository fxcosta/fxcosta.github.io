webpackJsonp([29],{"./node_modules/json-loader/index.js!./.cache/json/blog-an-intro-to-the-os-x-command-line.json":function(e,n){e.exports={data:{site:{siteMetadata:{title:"Felix Costa | Full-stack Developer",author:"Felix Costa"}},markdownRemark:{id:"/var/www/fxcosta-gatsby-v3/src/posts/command-line.md absPath of file >>> MarkdownRemark",html:'<p>This post is inspired by the terrible experiences I had when I first started working in the command line and is hopefully a better introduction to working with it in OS X. Before we go any further, I highly suggest you download and install <a href="http://www.iterm2.com/">iTerm2</a> because the default Terminal.app can be a bit little lacking. Once installed, keep it in your dock. <strong>It’s your new best friend.</strong></p>\n<p>If you’re not comfortable with that option, you can always use the default terminal found in OS X at: <code>Applications > Utilities > Terminal.app</code></p>\n<p><div>\n          <a\n            class="gatsby-resp-image-link"\n            title="original image"\n            href="/static/1a4e3e57a7e08eb6b8c98756b1f56e67-e56dd.png"\n            style="display: block"\n            target="_blank"\n          >\n            <div\n              class="gatsby-resp-image-wrapper"\n              style="position: relative; z-index: -1; "\n            >\n              <div\n                class="gatsby-resp-image-background-image"\n                style="padding-bottom: 41.935483870967744%;position: relative; width: 100%; bottom: 0; left: 0; background-image: url(\'data:image/png;base64,\'); background-size: cover;"\n              >\n                <img\n                  class="gatsby-resp-image-image"\n                  style="width: 100%; margin: 0; vertical-align: middle; position: absolute; box-shadow: inset 0px 0px 0px 400px white;"\n                  alt="Terminal.app"\n                  title=""\n                  src="/static/1a4e3e57a7e08eb6b8c98756b1f56e67-ba9a0.png"\n                  srcset="/static/1a4e3e57a7e08eb6b8c98756b1f56e67-125fe.png 163w,/static/1a4e3e57a7e08eb6b8c98756b1f56e67-637de.png 325w,/static/1a4e3e57a7e08eb6b8c98756b1f56e67-ba9a0.png 650w,/static/1a4e3e57a7e08eb6b8c98756b1f56e67-31247.png 975w,/static/1a4e3e57a7e08eb6b8c98756b1f56e67-38f66.png 1300w,/static/1a4e3e57a7e08eb6b8c98756b1f56e67-82702.png 1950w,/static/1a4e3e57a7e08eb6b8c98756b1f56e67-e56dd.png 2108w"\n                  sizes="(max-width: 650px) 100vw, 650px"\n                />\n              </div>\n            </div>\n          </a>\n          </div></p>\n<p>Throughout this post I’ll have code examples to highlight commands and their output. The examples will use the following documentation style:</p>\n<ul>\n<li>the <strong>commands</strong> you’ll be typing in will be the first line of every example and any lines that are not grayed out</li>\n<li>be sure to <strong>hit the enter key</strong> after you’ve confirmed the commands you’ve typed out, if not the terminal will not process the command</li>\n<li><strong>comments</strong> will start with a <code>#</code> and will be grayed out</li>\n</ul>\n<p>Here’s an example below that you can try out. The command is the first line and the expected result of the command is commented out in the second line.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token keyword">echo</span> <span class="token string">"hello world"</span>\n<span class="token comment" spellcheck="true"># hello world</span>\n</code></pre>\n      </div>\n<p>The command you just entered prints out or “echos” a message that you specify. That example isn’t that useful but the next examples will guide you through the core concepts needed to make the most of the terminal. These concepts include:</p>\n<ul>\n<li>Navigating the File System</li>\n<li>Manipulating the File System</li>\n<li>Helpful Tips &#x26; Tricks</li>\n<li>Shells &#x26; Settings</li>\n</ul>\n<h2>Navigating the File System</h2>\n<h3>Print Working Directory</h3>\n<p>First things first, when you run a command, it is always run from a folder on your system. This context or <strong>working directory</strong> is important to know since some commands are run relative to this. To show or “print” your current working directory, run the following command.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">pwd</span> something\n<span class="token comment" spellcheck="true">#  /Users/yourusername</span>\n</code></pre>\n      </div>\n<p>Don’t worry if your output doesn’t look like <code>/Users/yourusername</code>, the next command, <code>cd</code>, will get you back on track.</p>\n<h3>Change Directory - Part One</h3>\n<p>The change directory command helps you navigate through your file system. Run the following command to navigate to your home directory.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">cd</span>\n</code></pre>\n      </div>\n<p>There’s no output for this command but if you run <code>pwd</code> again you should see your home directory.</p>\n<h3>Open Current Directory</h3>\n<p>So now that we know where we are, the next command will open the Finder.app to help us relate what’s “around” us.</p>\n<pre><code class="language-bash"><span class="token keyword">open </span>.\n<span class="token comment"># A Finder window with your home directory should have opened up</span>\n</code></pre>\n<h3>List Directory Contents</h3>\n<p>Run the following command to list the current directory contents, which in our case would be our users main folders.</p>\n<pre><code class="language-bash"><span class="token keyword">ls</span>\n<span class="token comment"># Desktop Documents Downloads Library Movies Music Pictures Public Sites</span>\n</code></pre>\n<h3>Command Arguments</h3>\n<p>Any characters that come after a command can be considered an argument. Arguments may also be referred to as flags, options or parameters. Arguments alter the default options for a command.</p>\n<p>The output from the previous command was hard to read. To list directory contents in a proper list, add a <code>-l</code> argument to the previous <code>ls</code> command.</p>\n<pre><code class="language-bash"><span class="token keyword">ls </span>-l\n<span class="token comment"># Desktop</span>\n<span class="token comment"># Documents</span>\n<span class="token comment"># Downloads</span>\n<span class="token comment"># Library</span>\n<span class="token comment"># etc.</span>\n</code></pre>\n<p>To also include normally hidden files, like dotfiles -> <code>.example</code>, use the <code>-la</code> argument.</p>\n<pre><code class="language-bash"><span class="token keyword">ls </span>-la\n<span class="token comment"># .bash_history (example of hidden file)</span>\n<span class="token comment"># .DS_Store</span>\n<span class="token comment"># Desktop</span>\n<span class="token comment"># Documents</span>\n<span class="token comment"># etc.</span>\n</code></pre>\n<h3>Change Directory - Part Two</h3>\n<p>Now that we know arguments, let’s use them to move around our file system. Run the change directory command with <code>~/Documents</code> as an argument to navigate to your Documents folder. Note, the <code>~/</code> is a shorthand notation for the path to your users home directory. <code>~/</code> is equal to <code>/Users/yourusername</code>.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">cd</span> ~/Documents\n</code></pre>\n      </div>\n<p>Next, lets print our new working directory</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">pwd</span>\n<span class="token comment" spellcheck="true"># /Users/yourusername/Documents</span>\n</code></pre>\n      </div>\n<p>Next let’s print out the same stuff you’d see in Finder.app for the Documents directory</p>\n<pre><code class="language-bash"><span class="token keyword">ls </span>-la\n</code></pre>\n<p>Good stuff so far? Lets go a little bit further.</p>\n<h2>Manipulating the File System</h2>\n<p>Next up, we’ll be working from our Desktop directory to create a few child directories and files. Remember, you can use your Finder app to verify the commands you have run. Let’s go ahead and change our working directory to the Desktop.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">cd</span> ~/Desktop\n</code></pre>\n      </div>\n<h3>Make Directory</h3>\n<p>To create a directory or folder, you’ll use the <code>mkdir</code> command. Let’s create a directory on the desktop called “tmp” by running the following command.</p>\n<pre><code class="language-bash"><span class="token keyword">mkdir </span>tmp\n</code></pre>\n<h3>Create Files</h3>\n<p>Before we create files, navigate to the tmp directory we just created.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">cd</span> tmp\n</code></pre>\n      </div>\n<p>Now we can create a few files using the <code>touch</code> command.</p>\n<pre><code class="language-bash"><span class="token keyword">touch </span>foo.txt\n<span class="token keyword">touch </span>bar.txt\n</code></pre>\n<p>Next, let’s create another directory and create a file inside it. We can create files that are not in our current working directory by specifying the path to the file we want to create, <code>test/sample.txt</code>.</p>\n<pre><code class="language-bash"><span class="token keyword">mkdir </span>test\n<span class="token keyword">touch </span>test/sample.txt\n</code></pre>\n<p>Use <code>ls -l</code> or open this up in finder (<code>open .</code>) to visualize what we’ve created.</p>\n<h3>Copy</h3>\n<p>In order to copy files we’ll use the <code>cp</code> command. Try copying <code>foo.txt</code>.</p>\n<pre><code class="language-bash"><span class="token keyword">cp </span>foo.txt foo_copy.txt\n</code></pre>\n<p>Next let’s copy the <code>test</code> directory we made earlier and name this new copy <code>exam</code>. We’ll have to use the <code>-r</code> argument to ensure it recursively copies the directory and any files inside of it.</p>\n<pre><code class="language-bash"><span class="token keyword">cp </span>-r test exam\n</code></pre>\n<p>In this last <code>cp</code> example we’ll copy bar.txt into the exam directory.</p>\n<pre><code class="language-bash"><span class="token keyword">cp </span>bar.txt exam/bar.txt\n</code></pre>\n<p>Here’s a break down of the <code>cp</code> command</p>\n<ul>\n<li>the first argument after <code>cp</code> is the file or directory location</li>\n<li>the second argument is the destination of the file or directory</li>\n<li><code>cp filename.txt copyoffilename.txt</code> or <code>cp -r dir_path dir_path_copy</code></li>\n</ul>\n<h3>Move &#x26; Rename Files</h3>\n<p>The <code>mv</code> command allows you to move files or directories. Try it out by moving the file <code>foo.txt</code> inside of the <code>exam</code> directory.</p>\n<pre><code class="language-bash"><span class="token keyword">mv </span>foo.txt exam\n</code></pre>\n<p>Next, let’s move the <code>test</code> directory inside of the <code>exam</code> directory.</p>\n<pre><code class="language-bash"><span class="token keyword">mv </span>test exam\n</code></pre>\n<p>It sounds weird but we can also rename files by using the same <code>mv</code> command. Let’s practice renaming a file by changing <code>bar.txt</code> to <code>foobar.txt</code>.</p>\n<pre><code class="language-bash"><span class="token keyword">mv </span>bar.txt foobar.txt\n</code></pre>\n<p>Next, we’ll practice renaming a directory by changing exam to quiz.</p>\n<pre><code class="language-bash"><span class="token keyword">mv </span>exam quiz\n</code></pre>\n<h3>Remove/Delete</h3>\n<p>The <code>rm</code> command is used to delete files and/or directories. Unlike deleting a file or folder form the Finder app, this command does not move things to the Trash bin. Let’s try using the <code>rm</code> command to delete the file <code>foobar.txt</code>.</p>\n<pre><code class="language-bash"><span class="token keyword">rm </span>foobar.txt\n</code></pre>\n<p>Next, let’s delete the <code>test</code> directory within the <code>quiz</code> folder. We’ll pass <code>-rf</code> as an argument to remove everything within <code>quiz/test</code>.</p>\n<pre><code class="language-bash"><span class="token keyword">rm </span>-rf quiz/test\n</code></pre>\n<h2>Tips &#x26; Tricks</h2>\n<h3>History</h3>\n<p>Use the <code>up</code> and <code>down</code> arrow keys to go through your history of commands. To view your entire history of commands, use the <code>history</code> command.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">history</span>\n</code></pre>\n      </div>\n<p>To search your history of commands for something like “mkdir” (to see which directories you created), use <code>grep</code> in the following pattern.</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token comment" spellcheck="true"># history | grep "text to search"</span>\n<span class="token function">history</span> <span class="token operator">|</span> <span class="token function">grep</span> <span class="token string">"mkdir"</span>\n</code></pre>\n      </div>\n<p>In the previous command we used the <code>|</code> or pipe character. It’s used to pass in the output from the command on the left, <code>history</code> and pipes it into the next command, <code>grep mkdir</code>. I can’t stress how much this command combo has saved me.</p>\n<h3>Fix permissions</h3>\n<p>Permission errors are the most common problems I see people run into using the command line. Usually this happens when installing a language or language dependency. To fix this, run the following command to set your current user as the owner of the <code>/usr/local</code> directory.</p>\n<pre><code class="language-bash"><span class="token keyword">sudo chown </span>-R `whoami` /usr/local\n</code></pre>\n<h3>Add a shortcut to your editor</h3>\n<p>If you’re using Sublime Text, it’ll be very useful to have the <code>subl</code> command available to launch the app from the command line. With this you can pass in files or directories to open them directly in Sublime Text.</p>\n<pre><code class="language-bash"><span class="token keyword">ln</span> -s "/Applications/Sublime Text.app/Contents/SharedSupport/bin/subl" /usr/local/bin/subl\n</code></pre>\n<p>Example usage:</p>\n<pre><code class="language-bash"><span class="token comment"># to edit a file foo.txt</span>\n<span class="token keyword">subl</span> foo.txt\n\n<span class="token comment"># to open the current directory</span>\n<span class="token keyword">subl</span> .\n</code></pre>\n<h2>Wrapping up</h2>\n<p>Alright! I think that covers the most important skills for working with the terminal, navigating and manipulating the file system. With this knowledge you’ll have a better experience learning other <a href="https://computers.tutsplus.com/tutorials/40-terminal-tips-and-tricks-you-never-thought-you-needed--mac-51192">cool tips</a> or advanced stuff, like <a href="https://try.github.io">using git</a>. In a future post I’ll cover some other topics like shells, terminal settings, aliases and environment variables. As always, if you run into any issues with the commands above or if you have any feedback, feel free to leave a comment or <a href="https://github.com/eddywashere/eddywashere.com/issues/new">open</a> up a github issue.</p>',frontmatter:{date:"2015-07-14",title:"An intro to the OS X command line",tags:["osx","command line"]}}},pathContext:{slug:"/blog/an-intro-to-the-os-x-command-line/",collection:"posts"}}}});
//# sourceMappingURL=path---blog-an-intro-to-the-os-x-command-line-9330a705cf52c319773e.js.map